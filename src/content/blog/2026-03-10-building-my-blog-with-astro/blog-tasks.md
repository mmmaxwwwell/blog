# mmmaxwwwell's Blog — Tasks

## Status key
- `- [ ]` — not started
- `- [x]` — completed
- `- [?]` — blocked (see reason)
- `- [~]` — skipped / unnecessary (see reason)

## Phase 1: Project scaffolding
- [x] 1.1 Initialize Astro v5 project with MDX and Tailwind CSS v4 integrations. Configure `astro.config.mjs` with `site: 'https://mmmaxwwwell.github.io'`, `base: '/blog'`, MDX integration, and Tailwind. Set up `tsconfig.json` and `package.json`. — Done: Astro 5.18.1, @astrojs/mdx 4.3.14, Tailwind CSS 4.2.1 via @tailwindcss/vite plugin. Nix flake for Node 22. Build passes.
- [x] 1.2 Set up Tailwind config with retrowave color palette (neon pink, cyan, purple, dark/light backgrounds). Define CSS custom properties in `src/styles/global.css` for dark and light themes. Add Tailwind directives. — Done: Tailwind v4 `@theme` block with retrowave colors, CSS custom properties for dark (default) and light modes, base styles for body/links/code/selection.
- [x] 1.3 Create `BaseLayout.astro` — HTML shell with meta tags, global CSS import, dark/light theme script (check `localStorage` then `prefers-color-scheme`, apply class to `<html>`), and slot for page content. — Done: Layout at `src/layouts/BaseLayout.astro` with title/description props, inline theme detection script (localStorage → prefers-color-scheme → dark default), global CSS import. Updated `index.astro` to use layout. Build passes.
- [x] 1.4 Create `Header.astro` with site title/logo, nav links (Home, Blog, About), and `ThemeToggle.astro` component (dark/light toggle button, persists to `localStorage`). — Done: Sticky header with backdrop blur, desktop nav with active link highlighting, mobile hamburger menu with drawer, ThemeToggle with sun/moon icons. Added Header to BaseLayout. Build passes.
- [x] 1.5 Create `Footer.astro` with minimal footer (copyright, optional social links placeholder). — Done: Footer with dynamic copyright year, GitHub link (icon), RSS feed link (icon). Added to BaseLayout. Uses `mt-auto` for sticky footer. Build passes.

## Phase 2: Content collections & blog infrastructure
- [x] 2.1 Define content collection schema in `src/content.config.ts` with typed frontmatter (title, description, date, updated, category, tags, draft, heroImage). — Done: Used Astro v5 `glob` loader pattern. Schema at `src/content.config.ts`, blog posts in `src/content/blog/`. Added draft placeholder post. Build passes with no warnings.
- [x] 2.2 Create `PostLayout.astro` — blog post layout wrapping `BaseLayout`, displaying title, date, category link, tag pills, and rendered MDX content. Include Shiki syntax highlighting config in `astro.config.mjs`. — Done: Layout at `src/layouts/PostLayout.astro` with title, date, updated date, category link, tag pills, hero image, and prose styles for MDX content. Shiki was already configured in task 1.1; added CSS override in `global.css` to switch Shiki themes via `.light` class instead of `prefers-color-scheme`. Build passes.
- [x] 2.3 Create `PostCard.astro` component — card for post previews showing title, date, description, category, and tags. Style with retrowave accents. — Done: Card at `src/components/PostCard.astro` with rounded border, hover accent, date/category header, title, description, and tag pills. Title area is a single link, tags are separate links. Build passes.
- [x] 2.4 Create `TagList.astro` component — renders a list of tag pills/chips, each linking to `/blog/tags/[tag]/`. — Done: Extracted shared tag pill markup into `src/components/TagList.astro`. Updated PostCard and PostLayout to use it. Build passes.

## Phase 3: Pages
- [x] 3.1 Create home page (`src/pages/index.astro`) — recent posts (latest 10) using `PostCard`, with a subtle hero/tagline area. — Done: Hero section with title, tagline, retrowave gradient accent line. Lists up to 10 most recent non-draft posts using PostCard. Empty state message when no posts. Build passes.
- [x] 3.2 Create blog index page (`src/pages/blog/index.astro`) — all posts listed with `PostCard`, sorted by date descending. Include category filter links at the top. — Done: Blog index at `src/pages/blog/index.astro`. Shows all non-draft posts sorted by date desc. Category nav pills at top link to `/blog/category/{cat}/` pages (static-site friendly, no query params). "All" pill highlighted as active. Build passes.
- [x] 3.3 Create individual post pages (`src/pages/blog/[...slug].astro`) — dynamic route rendering each MDX post using `PostLayout`. Add prev/next post navigation. — Done: `src/pages/blog/[...slug].astro` with `getStaticPaths()`, `render()` for MDX content, prev/next nav at bottom. Posts sorted by date desc; "Newer" links left, "Older" links right. Build passes.
- [x] 3.4 Create category pages (`src/pages/category/[category].astro`) — dynamic route listing posts filtered by category. Generate static paths from all unique categories. — Done: `src/pages/category/[category].astro` with `getStaticPaths()`, category nav pills (current highlighted), filtered posts via PostCard. Build passes.
- [x] 3.5 Create tags index page (`src/pages/tags/index.astro`) — all tags displayed with post counts, linking to individual tag pages. — Done: `src/pages/tags/index.astro`. Collects all tags from non-draft posts, displays as pill links with post counts, sorted alphabetically. Empty state handled. Build passes.
- [x] 3.6 Create tag filter page (`src/pages/tags/[tag].astro`) — dynamic route listing posts filtered by tag. — Done: `src/pages/tags/[tag].astro` with `getStaticPaths()`, posts filtered by tag via PostCard, "All tags" back link. Build passes.
- [x] 3.7 Create about page (`src/pages/about.astro`) — bio section ("Software engineer riding the AI wave"), styled consistently with site design. — Done: About page with heading, gradient accent line, bio text, and GitHub link. Uses prose styling. Build passes.

## Phase 4: Polish & deploy
- [x] 4.1 Add responsive design — ensure all pages and components work well on mobile, tablet, and desktop. Test header nav collapse/hamburger on small screens. — Done: Added `sm:` breakpoint variants across all pages for headings, vertical padding, margins, gradient bars. PostCard mobile padding. Prev/next nav stacks vertically on mobile. Table overflow wrapper in PostLayout prose. Header hamburger already solid. Build passes.
- [x] 4.2 Add SEO basics — page titles, meta descriptions, Open Graph tags in `BaseLayout`. Add a `robots.txt` and basic `sitemap.xml` (Astro has a sitemap integration). — Done: Added OG and Twitter Card meta tags to BaseLayout (with optional image prop), canonical URL, sitemap link. Installed `@astrojs/sitemap` integration. Created `public/robots.txt`. PostLayout passes heroImage to BaseLayout for OG image. Build passes.
- [x] 4.3 Add RSS feed using `@astrojs/rss` — available at `/blog/rss.xml`. — Done: Installed `@astrojs/rss`, created `src/pages/rss.xml.ts` endpoint. Filters drafts, sorts by date desc, includes category + tags as RSS categories. Footer already linked to `/blog/rss.xml`. Build passes.
- [x] 4.4 Create GitHub Actions workflow (`.github/workflows/deploy.yml`) — build Astro site and deploy to GitHub Pages on push to `main`. — Done: Two-job workflow (build + deploy) using official GitHub Pages actions. Triggers on push to `main` and manual `workflow_dispatch`. Node 22, `npm ci`, `astro build`, uploads `dist/` artifact. Concurrency group prevents overlapping deploys. Build passes.
- [x] 4.5 Create 2-3 sample MDX posts covering different categories (one electronics, one software dev, one 3D printing) to demonstrate the blog with real content and test all features (tags, categories, code blocks, images). — Done: Created 3 posts: `2026-03-08-esp32-temperature-mqtt.mdx` (electrical), `2026-03-09-parametric-coaster-holder-openscad.mdx` (3d-printing), `2026-03-10-building-my-blog-with-astro.mdx` (software-dev). Fixed bug in `[category].astro` where `categories.sort()` inside `.map()` mutated the array mid-iteration, causing `3d-printing` category page to be skipped. Build passes with 21 pages.
- [x] 4.6 Final review — run `npm run build`, check for errors, test dark/light toggle, verify all links/routes work, check mobile layout. Fix any issues found. — Done: Build passes (21 pages, no errors). Fixed ThemeToggle bug — duplicate `id` attributes caused mobile toggle to not work. Switched to class-based selectors with `querySelectorAll` and guard against double-init. All links, routes, Shiki theming, and SEO meta verified correct.

## Phase 5: Content agent setup
- [~] 5.1 Write `CLAUDE.md` at project root with instructions for content-creation agent — Already exists and is fully populated with post creation instructions, frontmatter schema, writing style guidelines, category/tag conventions, image handling, commit conventions, and pre-publish checklist.
